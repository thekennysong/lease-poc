kind = "web"
previewPath = "/"
title = "Double — Lease Capitalization"
version = "1.0.0"
id = "artifacts/lease-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20888

[services.development]
run = "pnpm --filter @workspace/lease-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lease-app", "run", "build" ]
publicDir = "artifacts/lease-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20888"
BASE_PATH = "/"
